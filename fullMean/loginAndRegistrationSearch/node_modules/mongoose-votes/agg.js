conn = new Mongo();
db = conn.getDB('dunew');

var r = db.runCommand({
    aggregate: 'moves',
    pipeline: [
        {
            $project: {
                votes: {
                    $subtract: ['$upvotes', '$downvotes']
                }
            }
        }
    ]
});

